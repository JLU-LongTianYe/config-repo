eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
    myProduct:
      path: /myProduct/**
      serviceId: product
  ignored-patterns:
    - /product/productMsg
    - /myProduct/productMsg
  sensitive-headers:
management:
  security:
    enabled: false
